@javax.xml.bind.annotation.XmlType(name="{{className name}}")
@javax.xml.bind.annotation.XmlEnum({{enumValueNativeType}}.class)
public enum {{className name}} {

    {{#enumValues}}
    @javax.xml.bind.annotation.XmlEnumValue({{{.}}}) {{constantName .}}({{../enumValueNativeType}}.valueOf({{{.}}})){{#unless @last}}, {{else}};{{/unless}}
    {{/enumValues}}


    private {{enumValueNativeType}} value;

    {{className name}}({{enumValueNativeType}} v) {
        value = v;
    }

    @com.fasterxml.jackson.annotation.JsonValue public String value() {
        return value;
    }

    @Override
    public String toString() {
        return String.valueOf(value);
    }

    public static {{className name}} fromValue(String v) {
        for ({{className name}} b : {{className name}}.values()) {
            if (String.valueOf(b.value).equals(v)) {
                return b;
            }
        }
        return null;
    }
}
